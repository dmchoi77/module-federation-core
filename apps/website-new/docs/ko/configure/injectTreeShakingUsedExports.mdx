# injectTreeShakingUsedExports

`shared`에서 실제로 사용한 export 정보를 번들러 런타임에 주입할지 설정합니다.

- 타입: `boolean`
- 필수: 아니요
- 기본값: `undefined`

이 옵션을 사용하면 shared modules에서 실제로 사용된 export 정보를 번들러 런타임에 주입할 수 있습니다. 이를 통해 의존성 관리와 최적화를 더 정밀하게 수행할 수 있습니다.

`mode: 'server-calc'`를 사용한다면 이 옵션은 `false`로 설정하는 것을 권장합니다.
